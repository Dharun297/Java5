// Remove the first and last elements from an array of strings. (use methods)
let fruits = ["Apple", "Banana", "Orange", "Grapes"];
fruits.pop();
console.log(fruits);
fruits.shift();
console.log(fruits);
