// Count the number of words in a sentence (separated by spaces)
let myString = "This is a sample sentence";
const spacecount = myString.split(" ");
console.log(spacecount);
let total = spacecount.length;
console.log(total);